{
  "buildCommand": "cd src/app && npm install && npm run build",
  "outputDirectory": "src/app/dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    }
  ]
}
