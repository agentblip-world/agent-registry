{
  "buildCommand": "npm install && cd src/app && npm install && npm run build",
  "outputDirectory": "src/app/dist",
  "installCommand": "npm install",
  "functions": {
    "api/**/*.ts": {
      "runtime": "@vercel/node@3.2.21"
    }
  }
}
